{
  "exp_name": "run_04",
  "batch_size": 64,
  "model_type": "lstm",
  "model_params": {
    "input_size": 1,
    "hidden_size": 128,
    "num_layers": 1,
    "dropout": 0.3
  },
  "dataset_params": {
    "lmdb_path": "data/MSP/rtm_feats.lmdb",
    "data_source": "MSP",
    "items": ["intervals"]
  },
  "training_params": {
    "learning_rate": 1e-4,
    "weight_decay": 1e-4,
    "use_scheduler": false,
    "num_epochs": 1000,
    "plot_every": 5,
    "save_every": 10,
    "early_stop_patience": 100
  }
}