{
  "exp_name": "run_05",
  "batch_size": 64,
  "model_type": "lstm",
  "model_params": {
    "input_size": 2,
    "hidden_size": 128,
    "num_layers": 2,
    "dropout": 0.3,
    "pair_emb_dim": 64
  },
  "dataset_params": {
    "lmdb_path": "data/MSP/rtm_feats.lmdb",
    "data_source": "MSP",
    "items": ["envelope_spectrum"]
  },
  "training_params": {
    "learning_rate": 1e-4,
    "weight_decay": 1e-4,
    "use_scheduler": false,
    "num_epochs": 1000,
    "plot_every": 5,
    "save_every": 10,
    "early_stop_patience": 100
  }
}