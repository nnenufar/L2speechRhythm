{
  "exp_name": "C06",
  "batch_size": 64,
  "model_type": "spec_encoder",
  "model_params": {
    "posEmb_dim": 64,
    "cnn_out_channels": 128,
    "cnn_kernel_size": 3,
    "lstm_num_layers": 2,
    "lstm_hidden_size": 32,
    "max_audio_dur": 30,
    "dropout": 0.3
  },
  "dataset_params": {
    "lmdb_path": "data/globo/rtm_feats.lmdb",
    "data_source": "globo",
    "items": ["envelope_spectrum"]
  },
  "training_params": {
    "learning_rate": 1e-4,
    "weight_decay": 1e-4,
    "use_scheduler": false,
    "num_epochs": 1000,
    "plot_every": 5,
    "save_every": 10,
    "early_stop_patience": 100
  }
}